#include "inc.h"
class compiler{
    public:
        inline static bool comps = true;
        inline static bool regens_lang = true;
        inline static char preset[] = "c";
    private:
        compiler() {}
};